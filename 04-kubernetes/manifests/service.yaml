# 기본 Service 템플릿
# TODO: LoadBalancer / NodePort 변형 추가
apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: app
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
